import { test } from '@playwright/test';

test.describe('login.spec (mock)', () => {
  test('placeholder login test', async ({ page }) => {
    // placeholder - to be implemented during migration
  });
});
